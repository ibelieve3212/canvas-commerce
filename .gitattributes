# Git 行尾规范化。
#
# 关键：docker-entrypoint.sh 必须是 LF。若被 checkout 成 CRLF，
# alpine 的 /bin/sh 会把 \r 当成命令名的一部分，报
# "docker-entrypoint.sh: not found" —— 一个极难排查的报错。
* text=auto eol=lf

*.sh text eol=lf
Dockerfile text eol=lf
*.yml text eol=lf
*.yaml text eol=lf

# Windows 批处理必须是 CRLF，否则 cmd.exe 解析异常
*.bat text eol=crlf

# 二进制文件不做转换
*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.ico binary
*.node binary
*.wasm binary
*.db binary
